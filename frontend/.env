VITE_API_URL=http://localhost:8089/api/v1

# Google AdSense — dormant until BOTH are set (see src/config/ads.ts).
# Publisher ID from the AdSense dashboard, e.g. ca-pub-1234567890123456.
VITE_ADSENSE_CLIENT_ID=
# Master switch. Set to "true" only in production once AdSense-approved.
VITE_ADS_ENABLED=false
